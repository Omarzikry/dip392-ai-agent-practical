DIP392 Practical Task – Step 2 Submission
Student: Omar Zikry
Date: 08.05.2025

System: AI Study Assistant Agent (fully implemented)

The AI Study Assistant Agent CLI is complete. The agent validates user input, classifies intent,
and routes to either a SearchTool (keyword-based knowledge base lookup) or a CalculatorTool
(descriptive statistics). Results are returned as structured text with recommended next steps.

Programming concepts applied:
- Python dataclasses for typed return values (ValidationResult, SearchResult, StatsResult, AgentResponse)
- Type hints on all function signatures
- OOP: StudyAssistantAgent orchestrates 4 independent tool classes
- argparse for CLI entry point
- pathlib for cross-platform file path handling
- String processing for stop-word filtering and keyword relevance scoring
- Python statistics module for mean, median, standard deviation
- Exception handling for invalid input, missing files, division by zero
- pytest: 39 tests covering normal workflow, edge cases, and error paths
- Modular package design: src/study_agent/ with tools/ sub-package
- Git: 7 meaningful commits documenting project evolution

Tool integration: ValidatorTool always runs first. SearchTool uses FileReaderTool internally.
CalculatorTool is called for stats-related queries. All tools return typed dataclass objects.

Run: python3 -m study_agent.main "testing and deployment"
Test: python3 -m pytest tests/ -v
